# Copie para slurm/.env e preencha. NÃO commite o .env (já está no .gitignore).
# Tudo aqui é OPCIONAL — a pipeline roda sem nada disso usando dados públicos.

# Token do Hugging Face (opcional): evita avisos de rate-limit nos downloads públicos.
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx

# Overrides de infraestrutura (opcionais — têm default no .sbatch):
# CONTAINER_IMAGE=/raid/user_lucasalves/images/pytorch_2.2_cu121_py310.sif
# HOST_RAID=/raid/user_lucasalves
# PROJECT_SUBDIR=virtual-singer
