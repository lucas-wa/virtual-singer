# --- PyTorch (instalar com CUDA conforme sua GPU) ---
# Recomendado instalar separadamente escolhendo o build CUDA certo, ex.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
torch
torchaudio

# --- Áudio / DSP ---
librosa>=0.10
soundfile
numpy            # SEM pin <2.0 (era p/ o fairseq, já abandonado). numpy 2.x tem wheels p/ py3.12;
                 # fixar <2.0 forçava compilar do source e quebrava com pkgutil.ImpImporter no 3.12.
scipy
# NOTA: praat-parselmouth, pyworld e demucs saíram do core — não são usados no caminho
#       padrão (DSP + Seed-VC) e arriscam compilar do source / forçar numpy<2 no Python 3.12.
#       Para a mix com instrumental (Demucs), instale à parte:  pip install demucs
#       O Seed-VC traz suas próprias deps via third_party/seed-vc/requirements.txt.

# --- Partitura / música ---
music21                  # parse de MusicXML
pretty_midi              # parse de MIDI
mido

# --- Texto / fonemas (G2P) ---
phonemizer               # requer espeak-ng instalado no sistema
g2p_en                   # G2P para inglês (fallback offline)

# --- UI / utilidades ---
gradio>=4.0
tqdm
pyyaml
requests
huggingface_hub          # download de pesos pré-treinados
remotezip                # baixa só um cantor da VocalSet (scripts/get_sample_voice.py)

# NOTA 1: DiffSinger e RVC são clonados como repositórios (não são pacotes PyPI limpos).
#         scripts/setup_models.py cuida do download dos pesos; veja README.md.
#
# NOTA 2 (RVC): faiss-cpu e fairseq foram TIRADOS daqui de propósito — o fairseq quebra
#         o build com pip>=24.1 (metadata inválida do omegaconf<2.1). Instale o "extra do
#         RVC" à parte, com o pip rebaixado:
#             pip install "pip<24.1"
#             pip install fairseq==0.12.2 faiss-cpu
#         (no Colab, isto é uma célula separada — veja o notebook.)
